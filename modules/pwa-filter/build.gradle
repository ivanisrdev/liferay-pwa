configurations {
	compileOnly.extendsFrom(liferayReleaseApi, osgiBndLib, projectLombok)
	annotationProcessor.extendsFrom(projectLombok)
}

task copyJar(type: Copy) {
	from jar
	into '../dist'
}

build.finalizedBy copyJar